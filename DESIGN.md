# 回声唱片前台设计规范

## 体验方向

前台采用“轻科技薄荷声场”方向：参考图中取样的极浅薄荷白作为画布，原薄荷白转为辅助高光，深青灰形成稳定的信息层级，薄荷科技蓝只用于关键交互。玻璃材质只用于顶部导航和悬浮播放器。

## 设计令牌

- 薄荷科技蓝：`#6ED3CF`
- 极浅薄荷白（画布，参考图取样）：`#E9F8F5`
- 原薄荷白（辅助高光）：`#F3FEE7`
- 冷青灰：`#B7A992`
- 深青界面灰：`#3C5C5B`
- 边线：`rgba(60,92,91,.14)`
- 圆角：卡片 20px、内容大区 30–32px、悬浮播放器 36px
- 动效：常规反馈 180–260ms，只使用 opacity/transform；遵循 `prefers-reduced-motion`

## 组件原则

- Element Plus 负责输入框、按钮、轮播、分页、加载和反馈等基础交互。
- 悬浮播放胶囊采用淡鼠尾草绿：底色 `#DCE8DF`、文字与图标 `#38554B`、播放键 `#A9D8C3`；次要文字 `#546C60`、进度与选中态 `#3E7660`。配色由 `frontend/src/style.css` 的 `--player-*` 变量统一维护。
- 胶囊采用毛玻璃材质：68% 鼠尾草绿底色叠加轻白色渐变、24px 背景模糊、145% 饱和度、细白色高光边框和柔和阴影；不支持背景模糊时回退到实色底。
- 媒体卡片、歌曲行和播放器采用项目自有语义组件，所有页面复用。
- 品牌符号使用唱片、中心轴和唱臂组成的唱片机印象符。
- 全屏唱片机保留一条克制的金属唱臂：底座固定在唱片右上方，播放时落针，暂停时抬起；它是全屏播放器唯一的额外装饰，不延伸到歌词轨道。
- 歌曲缩略图使用圆形；歌单封面仍保留圆角方形，便于区分类别。
- 歌曲行不显示时长；行内播放入口使用 30px 薄荷底圆形按钮和 19px 实心播放图标，避免双层圆削弱识别度。
- 无后台封面时，歌单卡片从雾蓝/奶黄、雾桃粉/柔靛青、柔橘粉/烟雾紫、浅奶黄/柔炭灰四组参考色中按歌单稳定分配渐变；同一歌单刷新后不跳色。
- 歌单随机播放入口位于封面右下角，使用 57px 深青界面灰圆形播放按钮；桌面端悬浮或键盘聚焦时显现，触屏端保持可见，避免依赖悬浮才能操作。
- 桌面播放器固定为约 750×72px 的长椭圆胶囊，中央播放控件为 47px；上一首/下一首分置其两侧，随机与循环模式位于控制组最外侧。
- 全屏播放器使用“旋转唱片 + 纵向歌词轨道”的双栏声场；歌词区隐藏系统滚动轨道但保留滚轮、触控与键盘滚动，当前句只用细薄荷线和轻量字号变化强调。
- 只在 Header 和 Player 使用实时模糊；普通内容面使用不透明或半透明色，控制 GPU 开销。
- 图片缺失时显示渐变字标；数据缺失时显示空状态，不使用伪造业务数据。

## 页面结构

- 首页：轮播推荐、推荐歌单、最近上架。
- 歌单：歌单列表 → 歌单详情 → 播放全部/单曲播放。
- 歌手：歌手列表 → 歌手详情 → 播放歌手歌曲。
- 搜索：顶部搜索框 → 歌曲结果 → 单曲播放。
- 全局播放器：在所有前台路由持续存在，保存当前队列与播放状态。
- 全屏播放：从全局播放器进入，桌面双栏、移动端上下布局，展示同步歌词和完整播放控制。
