# 回声唱片 UX 合同

## 真实数据与状态

- 页面只使用 `/api/banners`、`/api/playlists`、`/api/singers`、`/api/songs` 及详情接口返回的数据。
- 首次加载保留内容高度并展示 loading；失败展示原因、后端端口提示和重试入口；空数据展示后台录入提示。
- 封面、头像、音频的相对地址统一补全为后端 `8081` 地址。

## 导航

- 顶部主导航包含发现、歌单、歌手；详情页保持所属一级导航高亮。
- 登录后可从顶部主导航进入“我的”页面，查看个人信息摘要与收藏歌单；未登录访问时返回登录页，并在登录后回到原页面。
- 搜索提交进入 `/search?q=关键词`，浏览器前进/后退可恢复查询。
- 小屏使用可展开导航，搜索框独占一行。

## 用户账号与收藏

- 注册成功后直接建立普通用户登录状态；登录成功后返回用户原本要访问的站内页面。
- 普通用户 JWT、昵称和资料快照保存在本地存储中，刷新后恢复；接口返回 401 时清除失效状态并返回登录页。
- 个人资料只允许用户修改自己的昵称、简介和头像，不能自行修改账号状态。
- 头像只接受 JPG、PNG、GIF、WebP，前后端均限制为 10MB；上传时展示进度，失败保留现有头像。
- 歌曲与歌单收藏以服务端记录为准；未登录点击收藏先进入登录页，完成登录后返回原页面。
- 退出登录只清理普通用户令牌和用户缓存，不影响管理员登录状态与公开页面浏览。
- 登录用户可在“我的”页面创建个人歌单；创建时歌单名称必填、简介可选，保存成功后留在当前页面并立即显示新歌单，失败时保留已填写内容并提供重试。
- 创建个人歌单时可直接上传封面；支持 JPG、PNG、GIF、WebP，前后端均限制为 10MB，上传时展示预览和进度，失败保留表单内容并允许重新选择；上传完成只更新当前表单字段，仍需点击“创建歌单”保存。
- 用户只能在“我的”页面编辑自己创建的歌单；编辑入口显示在自己的歌单卡片上，保存成功后留在当前页面并立即更新，失败时保留已填写内容并展示重试反馈。
- 登录用户可在歌曲列表行直接将歌曲加入自己创建的歌单；弹窗只列出当前用户的歌单，添加成功后关闭弹窗并提示结果，重复添加或服务失败时保留当前上下文并展示原因。

## 播放器

- 任意歌曲行点击后立即建立该列表为播放队列并开始播放。
- 歌单卡片的悬浮播放按钮只触发播放、不进入详情；点击后加载该歌单歌曲、随机排列播放队列并立即播放第一首，空歌单或加载失败时给出明确反馈。
- 支持播放/暂停、上一首、下一首、随机播放、列表循环、拖动进度、音量和静音控制；曲目结束按当前播放模式进入下一首或停止。
- 播放队列可查看、切换、移除单曲和清空；移除当前歌曲时保持剩余队列状态一致。
- 桌面播放器采用约 750×72px 长椭圆布局；音量滑杆收进弹层，避免持续占用胶囊空间。
- 音量入口和队列入口以紧凑图标放在播放器右侧，音量值在本机持久化。
- 音频失败时停止播放并在播放器内显示错误，不阻塞页面浏览。
- 移动端保留封面、曲名和播放/暂停，隐藏低优先级进度与音量控件。
- 当前有歌曲时可从悬浮播放器打开全屏播放；收起后保持歌曲、进度、队列与播放状态。
- 全屏歌词读取歌曲关联的 LRC 文件，按时间标签实时高亮并滚动；点击歌词行可跳转到对应时间。
- 全屏播放器的唱臂随播放状态联动：播放时落到唱片中心附近，暂停或无歌曲时抬起，不改变歌曲控制和歌词交互。
- 歌词缺失、格式无有效时间标签或加载失败时展示明确状态，但不影响音频播放。
- 后台歌曲管理支持上传单个不超过 2MB 的 `.lrc` 文件，前后端均校验格式，歌词为可选字段。

## 可访问性与性能

- 所有图像有替代文本，图标按钮有中文 `aria-label`，可操作项使用原生链接或按钮。
- 动效只改变 transform/opacity，并为减少动态效果偏好关闭非必要动画。
- 图片延迟加载；仅两个固定层使用 backdrop-filter，避免大面积持续模糊。

## 后台媒体录入

- 歌曲封面、歌手头像、歌单封面和用户头像统一支持本地图片直接上传，同时保留手动填写图片地址的能力。
- 后台图片只接受 JPG、PNG、GIF、WebP，前后端均限制为 10MB；上传时显示进度和预览，失败保留原图片与表单内容。
- 图片上传完成只更新当前表单字段，仍需点击表单保存按钮才会更新对应业务记录。
